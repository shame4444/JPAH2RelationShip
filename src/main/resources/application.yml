spring:
  datasource:
    url: jdbc:h2:mem:appdb
    username: sa
    password:

  h2:
    console:
      path: /h2
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    generate-ddl: true
server:
  port: 8090
